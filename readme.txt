语法接口：
solution = Solution()
result = solution.Hugmatch(centers)

输入参数:
{
  centers：所有图片中心的二维坐标组成的列表
    数据类型： [[int, int], [int, int], ...]
}

输出参数：
{
  result: 每一个图片中心坐标对应的正确行、列位置
    数据类型：[[int, int], [int, int], ...]
}
